# API use modules natives of NodeJS

## 📦 How to execute the project

```bash
  # Clone repository
  $ git clone

  # Enter in the repository
  $ cd api-without-framework

  # Install dependencies
  $ npm install

  # Start project
  $ npm run dev
```
